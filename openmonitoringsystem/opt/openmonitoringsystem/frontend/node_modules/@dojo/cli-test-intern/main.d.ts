import { Command } from '@dojo/cli/interfaces';
export interface TestArgs {
    all: boolean;
    browser: boolean;
    config?: string;
    functional: boolean;
    reporters?: string;
    testingKey?: string;
    secret?: string;
    userName?: string;
    unit: boolean;
    verbose: boolean;
    internConfig: string;
    node: boolean;
    filter: string;
}
/**
 * Compiled unit tests should exist when testing against a built application
 */
export declare function assertCompiledUnitTests(verbose: boolean): void;
declare const command: Command<TestArgs>;
export default command;
